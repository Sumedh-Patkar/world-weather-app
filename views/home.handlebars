<div class="container">
    <div class="row">
        {{#each cities_data_list }}
            {{#with this}}
                <div class="col-sm-6">
                    <div class="card my-5 city-details-morning">
                    <div class="card-body">
                        <h2>{{city-name}}, {{country}}</h2>
                        <h1 class="display-4 font-weight-bold">{{main_temp}} °C</h1>
                        <h2>{{weather-description}}</h2>
                        <form action="/city" method="GET">
                            <input type="hidden" id="city-name" name="city-name" value="{{city-name}}">
                            <button type="submit" class="btn btn-primary">Show full details</button>
                        </form>
                        
                    </div>
                    </div>
                </div>
            {{/with}}

        {{/each}}

    </div>
</div>